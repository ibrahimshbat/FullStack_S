QUIZ QUESTION
Given any string s, which of the following always have the same value as s? (Be careful, s could be '')

('a' + s)[1:] # ok

s[0] + s[1:] # Not ok Becuase if string is empty, s[0] leads to an error

s + '' # ok

s[0:] # ok